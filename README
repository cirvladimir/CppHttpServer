This is a c++ http server for handling http requests.
Features:
1. GET
2. POST

It does not support https.

Usage:
1. include server.h
2. compile with -pthread to link the multithreading pthread library (ex: g++ -pthread example.cpp)
3. check out example for the api